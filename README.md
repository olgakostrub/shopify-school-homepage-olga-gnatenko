# Shopify Test Task - Landing Page

Shopify Test Task - uses **HTML + SCSS + Webpack**.

---

## 🚀 Features
- Webpack 5 bundler
- SCSS support
- Live development server
- Asset optimization (images, CSS, JS)

---

## 📂 Project Structure
src/
├── js/ → JavaScript entry
├── scss/ → SCSS styles
├── images/ → Static images
└── index.html → HTML template
dist/ → Compiled output

## Setup

1. Install dependencies:
   `npm install`

2. Run development server:
    `npm start`

3. Build for production:
    `npm run build`

4. Cleaning
    `npm run clean`
    Removes the /dist folder.

5. Deploy to Github pages
    `npm run deploy`
    Deploys the page and makes it available here: https://olgakostrub.github.io/shopify-test-task/

# Comments and Future Improvements

- Thank you for a great and well-prepared test task! I will definitely complete it in the next days, as I really liked the approach, and want to complete the solution.
- If I had more time, I would complete other features in the following sequence: add dynamic elements (product highlights, slider, hero, popup) and implement mobile layout
- I would include responsive-loader to Webpack, make it generate responsive images automatically, and reference them where needed
- I would also split the html layout into smaller html files and bundle them with webpack, and split the large SCSS _layout.scss

Big thanks for this opportunity, probably at this point the test task does not have what is needs, but the experience and the process are anyway very rewarding. I do admire the approach your team has chosen to selecting the participants!
